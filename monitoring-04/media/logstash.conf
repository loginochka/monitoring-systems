input {
   beats{
   port => 5044
   }
}

filter {
}

output {
   elasticsearch {
   hosts => "http://elastic-hot:9200"
   index => "filebeat-test%{+YYYY.MM.DD}"
   user => "elastic"
   password => "GmzyysqGfhjkm322"
 }
}
